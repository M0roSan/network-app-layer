//Follow these steps 
//In VM
1. sudo python test.py
//this will create single switch topology, 3 hosts
//set ip for h1, h2, h3 to 10.0.0.1, 10.0.0.2, 10.0.0.3 respectively

//In mininet
1. h1 python server.py &
2. h3 python renderer.py &
3. h2 python controller.py -c 1 // h2 contacts to h1
4. h2 python controller.py -c 2 // h2 contacts to h3, and then h3 contacts to h1